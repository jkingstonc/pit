#pragma once

namespace pit {

	/*
	A job is a function queued up ready to be ran
	*/
	class Job{
		public:
		private:
	};

	/*
	The job pool stores 'hot' jobs (ready to run) and decides which one should run next
	*/
	class JobPool{
		public:
		private:
	};
}